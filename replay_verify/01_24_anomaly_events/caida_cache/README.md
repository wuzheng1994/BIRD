Place event-date CAIDA AS-relationship snapshots here.

Expected names:

```text
CAIDAASGraphCollector_YYYY.MM.01.txt
```

These files are not stored in git (about 134MB). Copy them from a local
`bgpy_24_events` delivery, or let BGPy download serial-2 data when the
matching cache is missing.
